import numpy as np
import pandas as pd
import xgboost as xgb
import random
import datetime as dt
import gc
import seaborn as sns #python visualization library
reticulate::repl_python()
reticulate::virtualenv_install(packages = "seaborn")
reticulate::virtualenv_install(env = "kaggle_zillow", packages = "seaborn")
reticulate::repl_python()
